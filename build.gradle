plugins {
    id 'io.freefair.aggregate-javadoc' version '4.1.6'
	id 'com.github.breadmoirai.github-release' version '2.2.9'
}

subprojects {
	group = 'de.lukaskoerfer.auto-implementation'
	version = '0.1'

	repositories {
		jcenter()
	}
}

githubRelease {
	authorization = System.getenv('')
}